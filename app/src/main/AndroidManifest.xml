<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project1_android_api">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
          COMMENTED OUT — the app is now single-Activity. Login and Sign Up are
          @Composable destinations in AppNavHost (LoginScreen.kt / SignUpScreen.kt),
          reached by navigation rather than by startActivity().

          Leaving these registered would imply the Activities are still live screens.
          Kept here rather than deleted so nothing is lost from the repo.

          TODO(team): delete this block along with LoginActivity.kt and
            SignUpActivity.kt once everyone has seen the change.

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="false"
            android:label="@string/title_activity_login"
            android:theme="@style/Theme.Project1_android_api"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ui.signup.SignUpActivity"
            android:exported="false"
            android:label="@string/title_activity_sign_up"
            android:theme="@style/Theme.Project1_android_api"
            android:windowSoftInputMode="adjustResize" />
        -->
    </application>

</manifest>